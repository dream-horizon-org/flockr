# Docker configuration - Use this when running in Docker
# The mock-api service is accessible via Docker network
url = "http://mock-api:8080/flockr/users/map-cohorts"
rateLimitPerSecond = 100
batchSize = 100
timeoutSeconds = 30
contentType = "application/json"
projectKey = "tenant1_100"

