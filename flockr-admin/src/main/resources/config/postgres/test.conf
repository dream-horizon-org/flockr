readerConfig.connectOptions.host = ${postgres.host}
readerConfig.connectOptions.port = ${postgres.port}
readerConfig.connectOptions.database = ${postgres.database}
readerConfig.connectOptions.user = ${postgres.user}
readerConfig.connectOptions.password = ${postgres.password}
readerConfig.connectOptions.cachePreparedStatements = true
readerConfig.poolOptions.maxSize = 5
readerConfig.poolOptions.maxWaitQueueSize = 2000
readerConfig.retryCount = 3

writerConfig.connectOptions.host = ${postgres.host}
writerConfig.connectOptions.port = ${postgres.port}
writerConfig.connectOptions.database = ${postgres.database}
writerConfig.connectOptions.user = ${postgres.user}
writerConfig.connectOptions.password = ${postgres.password}
readerConfig.connectOptions.cachePreparedStatements = true
writerConfig.poolOptions.maxSize = 3
writerConfig.poolOptions.maxWaitQueueSize = 2000
writerConfig.retryCount = 3
